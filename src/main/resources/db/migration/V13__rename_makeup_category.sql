-- V13: 대분류 이름 변경 — 메이크업 → 색조
--
-- 국내 뷰티 커머스는 이 분류를 대체로 "색조"라고 부른다. (D35)
-- 올리브영도 스킨케어 / 색조 / 헤어·바디 순으로 나눈다.
--
-- V12를 고치지 않고 새 마이그레이션을 쓴다. 이미 적용된 파일을 수정하면
-- Flyway 체크섬이 어긋나 기존 DB가 기동하지 못한다.
UPDATE product SET category = '색조' WHERE category = '메이크업';
